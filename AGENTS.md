# site configuration

for `www/`, `test/`, and every future site in this repository, keep editable configuration values at the top of each page's script, before functions or dom setup. keep css design tokens at the top of the stylesheet. group animation timings, palettes, and other tunable settings there too.
